import SwiftUI

struct NoMixesView: View {
  
}

extension NoMixesView {
  var body: some View {
    Text("No saved mixes")
      .foregroundColor(.secondary)
      .padding()
  }
}

#Preview {
  NoMixesView()
}
